# Findings: Flutter web `--web-content-hash` red-team

Status legend: **BLOCKER** ships broken users · **HIGH** stale/mixed app in a
common configuration · **MEDIUM** needs docs or tooling · **LOW** cosmetic /
edge · **INFO** confirmed-good behavior worth recording.

Each finding: severity, target (`master` = Phase 1 as shipped, `phase3` =
Phases 1–3 branch), repro (scenario id + command), expected vs actual, and a
recommendation (tool change / docs / hosting guidance / `flutter_web_cache_check`).

_Populated from `results/*/summary.md` as scenarios run._
