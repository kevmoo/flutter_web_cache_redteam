name: sample_pkg
description: A dependency package that ships its own assets (packages/sample_pkg/...).
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"
dependencies:
  flutter:
    sdk: flutter
flutter:
  assets:
    - assets/pkg.txt
    - assets/pkg.png
